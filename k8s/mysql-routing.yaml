apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-routing
  labels:
    app: mysql-routing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-routing
  template:
    metadata:
      labels:
        app: mysql-routing
    spec:
      containers:
        - name: mysql-routing
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "windowcloud"
            - name: MYSQL_DATABASE
              value: "routesdb"
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost", "-pwindowcloud"]
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost", "-pwindowcloud"]
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 5
          volumeMounts:
            - name: mysql-routing-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-routing-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-routing
spec:
  selector:
    app: mysql-routing
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP