apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: autocompany-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: http
      path: /health
      interval: 15s