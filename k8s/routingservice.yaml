apiVersion: apps/v1
kind: Deployment
metadata:
  name: routing-api
  labels:
    app: routing-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: routing-api
  template:
    metadata:
      labels:
        app: routing-api
    spec:
      containers:
        - name: routing-api
          image: routing-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ConnectionStrings__DefaultConnection
              value: "Server=mysql-routing;Port=3306;Database=routesdb;User=root;Password=windowcloud;"
            - name: ConnectionStrings__redis
              value: "redis:6379"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: routing-api
spec:
  selector:
    app: routing-api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP